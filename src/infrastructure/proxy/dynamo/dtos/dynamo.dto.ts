export interface TopicoConfig {
  topico: string;
  versao: number;
  arn: string;
  schema: any;
}

export interface TopicoVerificacaoDeConta {
  email: string;
  codigo: string;
}

export interface TopicoBoasVindas {
  email: string;
}
